{
  "scripts": {
    "build": "browserify -p esmify -p common-shakeify -t unassertify -t uglifyify --debug index.js > public/bundle.js"
  },
  "dependencies": {
    "browserify": "^17.0.0",
    "common-shakeify": "^0.6.2",
    "esmify": "^2.1.1",
    "htm": "^3.1.0",
    "preact": "^10.5.14",
    "uglifyify": "^5.0.2",
    "unassertify": "^2.1.1"
  }
}
